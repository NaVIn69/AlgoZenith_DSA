#include<bits/stdc++.h>
using namespace std;
#define int long long
#define endl "\n"
void solve(){
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    // binary search on every start
    // means every start se find the optimal r
    //for each l we have to find the optimal r


}
signed main(){
    solve();
}